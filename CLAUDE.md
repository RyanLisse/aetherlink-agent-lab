# Participant lab instructions

@intent.md
@progress.md
@AGENTS.md

`intent.md` and `progress.md` are project convention files; keep these imports
explicit so the participant outcome, current-state board, and contribution
rules load together before changing an exercise artifact.
